version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:12.14
    steps:
      - checkout
      - sonarcloud/scan
          cache_version: 2
orbs:
  sonarcloud: sonarsource/sonarcloud@1.0.1

workflows:
  main:
    jobs:
      - build:
          context: sonarcloud
